# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['encode_gui.py'],
    pathex=[],
    binaries=[('ffmpeg', '.')],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='mobipeg',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['/Users/larsen/Downloads/logo.png'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='mobipeg',
)
app = BUNDLE(
    coll,
    name='mobipeg.app',
    icon='/Users/larsen/Downloads/logo.png',
    bundle_identifier=None,
)
