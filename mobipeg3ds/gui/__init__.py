"""Mobipeg 3DS GUI -- Experimental Preview.

Every ffmpeg argument comes from mobipeg3ds.backend.resolve_args(). This
package contains presentation logic only -- no argument construction, no
encoder-parameter decisions -- so the CLI and GUI can never silently drift
apart on what a given job actually does.
"""
